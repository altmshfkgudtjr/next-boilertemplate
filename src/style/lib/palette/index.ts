// styels
import lightTheme from 'style/lib/palette/light';
import darkTheme from 'style/lib/palette/dark';
// types
import { ThemePalette } from 'style/types/Palette';

/** 테마 색상표 SET */
const themePalette: ThemePalette = {
  light: lightTheme,
  dark: darkTheme,
};

export default themePalette;
